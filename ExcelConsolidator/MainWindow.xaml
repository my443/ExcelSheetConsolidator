<Window x:Class="ExcelConsolidator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelConsolidator"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>


        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#F3F3F3" LastChildFill="False">
            <Label Content="Excel Extractor" 
                   DockPanel.Dock="Left" 
                   VerticalAlignment="Center" 
                   Margin="10,0" 
                   FontWeight="Bold"/>

            <Button Content="About" 
                    DockPanel.Dock="Right" 
                    VerticalAlignment="Center" 
                    Padding="10,2" 
                    Margin="10,0"/>
        </DockPanel>

        <Grid Grid.Row="1" Margin="20">
            <StackPanel>
                <StackPanel Margin="0,10">
                    <Button Content="Select Source Folder" Click="SelectSourceFolder_Click" Width="200" HorizontalAlignment="Left"/>
                    <Label Content="{Binding FolderPathDisplay}" Background="#FF7F1616" Foreground="White" Width="400" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Margin="0,10">
                    <Button Content="Select Template File" Click="SelectTemplateFile_Click" Width="200" HorizontalAlignment="Left"/>
                    <Label Content="{Binding TemplateFilePath}" Background="#FF7F1616" Foreground="White" Width="400" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Margin="0,10">
                    <Button Content="Select Output File" Click="SelectOutputFile_Click" Width="200" HorizontalAlignment="Left"/>
                    <Label Content="{Binding OutputFilePath}" Background="#FF7F1616" Foreground="White" Width="400" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                </StackPanel>

                <Button Content="Consolidate Workbooks" 
                    HorizontalAlignment="Right" 
                    Margin="0,30,0,0" 
                    Height="49" Width="232" 
                    Click="Button_Click" 
                    IsEnabled="{Binding SubmitIsEnabled}"/>
            </StackPanel>
        </Grid>
        <StatusBar Grid.Row="2">
            <Label Content="{Binding StatusMessage}"></Label>
        </StatusBar>
    </Grid>
</Window>
